# ===========================================
# UNATTENDED SETUP (First-start only)
# ===========================================
# These variables only apply during initial container startup
# Remove them after first setup to avoid exposing credentials

# Enable unattended setup
#INIT_ENABLED=true

# Admin credentials (Group 1 - required together)
#INIT_USERNAME=admin
#INIT_PASSWORD=your-secure-password-here

# WireGuard host and port (Group 1 - required together)
# Use your public IP or domain name
#INIT_HOST=vpn.example.com
#INIT_PORT=51820

# DNS servers for VPN clients (Group 2 - optional)
# Comma-separated list
#INIT_DNS=1.1.1.1,8.8.8.8

# Network CIDR ranges (Group 3 - must set both together)
#INIT_IPV4_CIDR=10.8.0.0/24
#INIT_IPV6_CIDR=2001:0DB8::/32

# Allowed IPs (optional)
# Traffic that will be routed through the VPN tunnel
# Examples:
#   - All traffic (VPN as gateway): 0.0.0.0/0,::/0
#   - Only LAN access: 192.168.124.0/23,10.32.33.0/24
#   - Default if not set: 0.0.0.0/0,::/0
# Comma-separated list
#INIT_ALLOWED_IPS=0.0.0.0/0,::/0

# ===========================================
# RUNTIME CONFIGURATION
# ===========================================

# Web UI Port (TCP)
# Default: 51821
#PORT=51821

# Web UI Listen Address
# Default: 0.0.0.0
#HOST=0.0.0.0

# Serve UI over HTTP without HTTPS
# Set to true only if behind a reverse proxy with HTTPS
# Default: false
#INSECURE=false

# ===========================================
# WIREGUARD SETTINGS
# ===========================================

# WireGuard VPN port exposed on the host (UDP)
# This is the port you need to forward in your firewall
# Must match INIT_PORT for clients to connect correctly
# Container always uses 51820 internally
# Default: 51820
#WG_PORT=51820

# Docker container IP address within the wg network
# Must be within the subnet defined in docker-compose.yml
# Change this if the default .2 is already in use on your system
# Default: 10.32.33.2
#WG_CONTAINER_IP=10.32.33.100

# Disable IPv6 support (removes ip6tables rules)
# Default: false
#DISABLE_IPV6=false

# Force use of specific WireGuard implementation
# Options: wg, awg (AmneziaWG)
# Default: auto-detect
#OVERRIDE_AUTO_AWG=wg
